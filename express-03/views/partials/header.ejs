<header>
    <h1>here are our register homes:</h1>
    <ul>
        <% requestItems.forEach(homes=> { %>
            <li>
                <%= homes %>
            </li>
            <% }) %>
    </ul>
</header>